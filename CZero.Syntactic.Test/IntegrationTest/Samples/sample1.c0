fn bar() -> int { // <Function> <BlockStatement>
    let i: int; // <LetDeclarationStatement>
    i = getint(); // <AssignExpression>
    while i > 0 { // <BlockStatement>
        i = i - 1; // <AssignExpression>
        if i <= 0 { // <BlockStatement>
            return i;
        }
    }
}