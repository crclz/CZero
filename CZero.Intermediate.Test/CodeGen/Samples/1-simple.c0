let a:int=1; 
const b: double=2.5; 
let c:double;

fn printf(x:int, const y:int, z: double) -> double {
    a=-321+3+3213 as int;
    x = x+x+x*x+y+z as int;
    z = 3.14*10.2 + c*b;
    return z;
}

fn Notmain(const m:int, const n:double) -> void {
    m;
    //return a;
}

fn main()->void {
    Notmain(3,3.5);
}

fn empty() -> void {

}

fn empty2() -> void {
    return;
}